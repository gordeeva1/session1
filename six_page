import 'package:flutter/material.dart';

class six_page extends StatelessWidget {
  const six_page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        padding: EdgeInsets.all(8),
        child: Column(
            mainAxisAlignment: MainAxisAlignment.spaceEvenly,
            children: <Widget>[
              const Text("Введите код из E-mail", style: TextStyle(color: Colors.black, fontSize: 20)),
        TextField(
          decoration: InputDecoration(
            border: OutlineInputBorder(),
            hintText: ' ',
          ),
        ),
           const Text("Отправить код повторно можно будет через 59 секунд", style: TextStyle(color: Colors.black38, fontSize: 14)),
        ],
      ),
    )
    );
  }
}
