import 'package:flutter/material.dart';
class nine_page extends StatelessWidget {
  const nine_page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Container(
        padding: EdgeInsets.all(8),
          child: Center(
          child: Column(
             mainAxisAlignment: MainAxisAlignment.spaceEvenly,
              children: <Widget>[
                Row(
                  mainAxisAlignment: MainAxisAlignment.spaceBetween,
                  crossAxisAlignment: CrossAxisAlignment.start,
                  children: <Widget>[
                  const Text( "Создание карты пациента", style: TextStyle(fontSize: 20,fontWeight: FontWeight.normal),),
                  TextButton(
                    onPressed: () {},
                    child: const Text(
                     "Пропустить",
                      style: TextStyle(
                       fontSize: 20, fontWeight: FontWeight.normal),
                  ),
                  ),
                 const Text("Без карты пациента вы не сможете заказать анализы.", style: TextStyle(fontSize: 14, color: Colors.white30),),
                    const Text("В картах пациентов будут храниться резултаты анализов вас и ваших близких.", style: TextStyle(fontSize: 14, color: Colors.white30),)
    ],
    ),
    ],
    ),
    ),
    ),
    );

  }
}
